# 小红书「作者关闭保存」逆向分析与绕过思路

> 目标：搞清小红书新功能「作者可关闭笔记图片下载/保存」是怎么实现的，并在自己设备上解除这个客户端限制。
> 版本：小红书 `com.xingin.xhs` v9.33.4（2026-06）。设备：已 root + LSPosed 的 Android。
> 仅供个人学习与逆向研究，请尊重原作者版权与平台规则。

---

## 0. 现象与第一性判断

现象：某些笔记长按图片没有「保存到相册」或点了保存失败，且**只有特定作者设置后**他发布的图片才不能存——同一个 App、同样的保存按钮，行为因笔记而异。

由此先做判断（不急着开 IDA 啃 .so）：

- 「因笔记而异」⇒ 限制开关是**跟着单条笔记的数据走的**，几乎必然是**服务器下发的一个字段**。
- 客户端只是「读到这个字段 → 决定让不让保存」。这种**业务标志的判断，绝大多数在 Java/Kotlin 层**，不在 native。
- 所以**先啃 Java 层**，native（.so）留作兜底。结论：本案最终**完全在 Java 层解决，没碰 .so**。

> 经验：先按「数据从哪来、谁来判断」反推所在层级，能省掉大量盲目分析。功能性/UI 限制基本在 Java；只有签名、加密、风控、强校验才下沉 .so。

---

## 1. 抠 APK

```bash
adb shell pm path com.xingin.xhs                       # 找 base.apk 路径
adb pull <base.apk 路径> xhs-9.33.4.apk                 # 拉到本机(单体包 ~167MB)
unzip -l xhs-9.33.4.apk 'lib/arm64-v8a/*'              # 先扫一眼 native 库构成
```

native 库看了一圈全是基础设施（播放器/ffmpeg/RN-hermes/cronet 网络/db/longlink），没有像「保存控制」的专用库 → 进一步印证不在 .so。

---

## 2. dex 字符串侦察（不等 jadx，先快速定位）

jadx 解一个 167MB 的大 App 很慢（十几万类）。先用**二进制 grep 直接搜 dex**，几秒就能锁定关键词：

```bash
unzip -o xhs-9.33.4.apk 'classes*.dex'                 # 20 个 dex
# 中文线索
for kw in 无法保存 保存失败 已关闭 作者; do grep -ac "$kw" classes*.dex; done
# 英文标志字段候选(关键)
for kw in disableSave canSave noSave savable disable_save image_save watermark saveImage; do
  grep -ac "$kw" classes*.dex
done
```

命中：**`disableSave`（9 次）**、`disable_save`、`canSave`、`savable`、`watermark`…
其中 `disableSave` / `disable_save` 最像「关闭保存」的标志字段名。

接着看它周边的字符串（dex 里同类的字符串常聚在一起），抠出类名上下文：

```bash
grep -aboF disableSave classes6.dex | head | while IFS=: read off _; do
  dd if=classes6.dex bs=1 skip=$((off-60)) count=160 2>/dev/null | strings -n 3
done
```

冒出两条决定性线索：

- `MediaSaveConfig(disableSaveMedia=…, disableWaterMark=…)` —— 一个**笔记级「保存配置」类**。
- `download blocked, disableSaveMedia=true, noteId=` —— **运行时拦截下载的日志**，命中即拦截。

---

## 3. jadx 定位模型类（确认字段与 JSON 映射）

```bash
jadx -d xhs_jadx --no-res xhs-9.33.4.apk &             # 后台跑(慢)
# 关键类很快就落地:
cat xhs_jadx/sources/com/xingin/entities/MediaSaveConfig.java
```

模型类（去混淆后）：

```java
public class MediaSaveConfig implements Parcelable {
    @ad.c("disable_weibo_cover") private boolean disableCoverWeibo;
    @ad.c("disable_save")        private boolean disableSaveMedia;   // ★ 作者关闭保存 = true
    @ad.c("disable_watermark")   private boolean disableWaterMark;

    public final boolean b() { return this.disableSaveMedia; }       // getter
    public final boolean c() { return this.disableWaterMark; }
}
```

**服务器 JSON 字段就是 `disable_save`**（`@ad.c(...)` = Gson `@SerializedName`），作者开启后下发 `true`，反序列化进 `disableSaveMedia`，对外用 getter `b()` 读。

---

## 4. 定位检查点（确认是「调 getter」还是「直接读字段」）

这一步决定怎么绕：如果检查调的是 `b()`，hook `b()` 就够；如果直接读字段或被内联，得换地方。

全量 jadx 太慢，**单独只解含拦截日志的那个 dex**（快很多）：

```bash
jadx -d xhs_c6 --no-res classes6.dex
grep -rl "download blocked" xhs_c6/                    # -> sources/ap8/b.java
```

`ap8/b.java`（`DownloadController.onDownloadClick`，长按面板「保存」点击处理）：

```java
// 长按图片点保存:
log("onDownloadClick, noteId=" + note.Q9().A0()
        + ", disableSaveMedia=" + note.Q9().e1().b());
...
if (note.Q9().e1().b()) {                               // ★ 检查 disableSaveMedia
    log("download blocked, disableSaveMedia=true, noteId=" + note.Q9().A0());
    k.h(i1.d(2131828069));                              // 弹 toast(作者已关闭保存)
    return Unit.INSTANCE;                                // ★ 直接 return = 拦截
}
// 否则才往下执行真正的保存/下载
```

取值链 `note.Q9()`(笔记) → `.e1()`(MediaSaveConfig) → `.b()`(disableSaveMedia)。
**检查确实是调 getter `b()`** ⇒ 只要让 `b()` 恒返回 `false`，`if` 永不命中，跳过 `return`，往下走真正保存。

---

## 5. 为什么用 LSPosed 而不是 Frida

先试了 Frida 动态确认/绕过：

```bash
frida -U -p <xhs pid> -l hook.js     # -> Failed to attach: ... frida-server: closed
```

`frida-ps -U` 能列进程（server 正常），但 **attach 小红书主进程就被掐断** —— 小红书带反 frida（`libmsaoaidsec.so` 安全库），专挡外部调试器注入。

关键认知（和飞书那套反 frida 一样）：

> 反 frida 只挡**外部 frida 注入**；**LSPosed 模块是被 App 自己的 ClassLoader 加载、以 App 身份在进程内运行的，早过了反 frida**。

所以绕过走 **LSPosed 模块**，稳、过反调试。实测注入主进程成功，反 frida 没拦住。

---

## 6. 实现（一个方法 hook）

LSPosed 模块，作用域 `com.xingin.xhs`，把 getter 强制 `false`：

```java
XposedHelpers.findAndHookMethod(
    "com.xingin.entities.MediaSaveConfig", lpparam.classLoader, "b",
    new XC_MethodHook() {
        @Override protected void afterHookedMethod(MethodHookParam p) {
            if (Boolean.TRUE.equals(p.getResult())) p.setResult(Boolean.FALSE);
        }
    });
// c() = disableWaterMark 同理一并去掉强制水印
```

完整代码见 [`app/src/main/java/com/chekayo/xhssavefix/XhsSaveFix.java`](app/src/main/java/com/chekayo/xhssavefix/XhsSaveFix.java)。

---

## 7. 验证

LSPosed 启用 + 勾小红书作用域 + 重启 App，logcat：

```
LSPosedFramework: (com.xingin.xhs)[com.chekayo.xhssavefix] [xhs-savefix] hooked MediaSaveConfig.b() (disableSaveMedia) -> false
```

打开受限笔记 → 长按图片 → 保存到相册：**保存成功**（原本弹 toast 拒绝）。✅

---

## 方法论小结（可复用）

1. **先按数据流反推层级**：因笔记/因用户而异的开关 = 服务器字段 + 客户端判断，先啃 Java，别上来就啃 .so。
2. **dex 二进制 grep 先行**：大 App jadx 慢，先 `grep` dex 锁定标志字段名（中英文都试），再针对性 jadx。
3. **字符串即路标**：模型类带 `toString()`、检查点带拦截日志，这些字符串直接把你带到类和方法。
4. **确认 getter vs 字段**：决定 hook 点。本案检查走 getter，hook getter 最省。
5. **反 frida ≠ 反 Xposed**：外部 Frida 被挡时，LSPosed 进程内 hook 往往照样通。
6. **最小改动**：能一个方法 hook 解决，就不碰更多东西，稳。
